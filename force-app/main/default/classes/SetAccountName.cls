public with sharing class SetAccountName {
    public SetAccountName() {

    }

    // So this of this like a domain... activity layer? I am not sure what to call it. 

    // So at the end of the day... this really should be ran using a command interface... how should that command interface look like?
}
